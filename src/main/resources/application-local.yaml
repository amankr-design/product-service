spring:
  application:
    name: product-service

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/ecommerce_product_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: springuser
    password: Dell@202301!

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  cache:
    type: redis
  data:
    redis:
      host: localhost
      port: 6379

jwt:
  secret: mySecretKeyForJwtAuthentication12345678901234567890
  expiration: 86400000

server:
  port: 8081

